Sehr <%= if @customer.designation == "female" then "geehrte Frau #{@customer.last_name}"; else "geehrter Herr #{@customer.last_name}" end  %>,

wir haben folgende Karten für Sie vorgemerkt:
<% @order.getSeatsPerTable.each { |table, seats|  %>
<%= seats.length -%>
<%= if seats.length > 1 then " #{table.seat_description}n"; else " #{table.seat_description}" end -%>
<%= if table.orientation != "promenade" then " an #{table.caption}" end -%>
<%= unless seats.length == 10 then " à #{number_to_currency(seats.first.price, :unit => "", :separator => ",", :delimiter => "")} €" end -%>
<%= unless seats.length == 10 then " = #{number_to_currency(seats.first.price * seats.length, :unit => "", :separator => ",", :delimiter => "")} €" else " = #{number_to_currency(table.price, :unit => "", :separator => ",", :delimiter => "")} €" end -%>

<%	}  %>

Bitte überweisen Sie <%= "#{number_to_currency(@order.amount, :unit => "", :separator => ",", :delimiter => "")} €" -%> auf das folgenden Konto:
Kontoinhaber: Theresienball
Konto-Nr.: 60 12 94 14 29
BLZ: 100 500 00 (Berliner Sparkasse)

Geben Sie bitte als Verwendungszweck Ihren Namen an, damit wir Ihre Zahlung zuordnen können.
Sobald ihre Zahlung eingegangen ist, sind die Plätze für Sie reserviert.
<%= if @order.delivery_method == "Hinterlegung" then "Die Karten werden an der Abendkasse für Sie hinterlegt." end -%>
<%= if @order.delivery_method == "Email" then "Die Karten werden Ihnen bis Mitte Oktober per E-Mail zugesandt." end -%>


Wir freuen uns auf einen vergnüglichen Ballabend.

Herzliche Grüße
Julien Bergner
(für das Theresienball-Team)